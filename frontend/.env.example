# Vite environment variables
#
# Recommended (dev): rely on Vite proxy for /api requests.
# Only set VITE_API_BASE_URL if you want axios to call a full URL directly.

# Backend URL for Vite dev proxy (used by vite.config.ts)
VITE_API_PROXY_TARGET=http://localhost:5000

# Optional: axios base URL (leave empty to use Vite proxy)
# VITE_API_BASE_URL=http://localhost:5000
